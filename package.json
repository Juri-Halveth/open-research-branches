{
  "name": "open-research-branches",
  "version": "0.11.1",
  "description": "Public-safe, independently continuable research and software branches.",
  "private": true,
  "type": "module",
  "license": "SEE LICENSE IN LICENSES.md",
  "repository": "github:Juri-Halveth/open-research-branches",
  "keywords": [
    "open-research",
    "reproducibility",
    "synthetic-data"
  ],
  "scripts": {
    "test": "node scripts/validate-public-manifest.mjs && node scripts/run-tests.mjs && node --test scripts/provenance-snapshot.test.mjs && node --test scripts/audit-star.test.mjs && node scripts/validate-catalog.mjs && node scripts/prepublish-check.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
